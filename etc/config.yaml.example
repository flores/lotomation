auth:
  ninjablocks: '' # api token
location:
  doorcouch: # some zone name
    min: -84 # lower threshold for zone, in rssi
    max: -80 # upper threshold for zone, in rssi
    devices: [ 'device1', 'device2' ] # array of ninjablock devices
  middle:
    min: -77
    max: -60
    devices: [ 'stuff' ]
beacon: # mac addresses of bluetooth le devices
  fitbit: '' 
